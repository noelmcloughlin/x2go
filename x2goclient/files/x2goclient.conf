{% set indent_increment = 4 %}

{%- macro x2goclient_block(value, key=None, operator=' ', delim=';', ind=0) -%}
    {%- if value != None -%}
    {%- if value is number or value is string -%}
        {{ key|indent(ind, True) }}{{ operator }}{{ value }}{{ delim }}
    {%- elif value is mapping -%}
        {{ key|indent(ind, True) }}{{ operator }}{{ '{' }}
        {%- for k, v in value.items() %}
        {%- if k != 'include' %}
{{ x2goclient_block(v, k, operator, delim, (ind + indent_increment)) }}
        {%- endif %}
        {%- endfor %}
        {%- if 'include' in value.keys() %}
{{ x2goclient_block(value['include'], 'include', operator, delim, (ind + indent_increment)) }}
        {%- endif %}
{{ '}'|indent(ind, True) }}
    {%- elif value is iterable -%}
        {%- for v in value %}
{{ x2goclient_block(v, key, operator, delim, ind) }}
        {%- endfor -%}
    {%- else -%}
        {{ key|indent(ind, True) }}{{ operator }}{{ value }}{{ delim }}
    {%- endif -%}
    {%- else -%}
    {%- endif -%}
{%- endmacro -%}

# Default x2goclient client configuration
#
# **** DO NOT EDIT THIS FILE ****
#
# This file is managed by Salt.

{% for key, value in config.items() %}
{{ x2goclient_block(value, key) }}
{%- endfor -%}
